import {Schema} from '../../components/schema-renderers';
import CONFIG_SCHEMA from '../../components/schemas/config-schema';
import FOREIGN_CONFIG_SCHEMA from '../../components/schemas/foreign-config-schema';

# Configuration

## `config.json`

### Example File

{/* The following example is synced automatically with the file in `config/config.template.json` whenever running the config schema export. */}

```json
{
  "general": {
    "config_revision": 1,
    "software_version": "1.0.0",
    "system_identifier": "my-system-1"
  },
  "logging_verbosity": {
    "file_archive": "DEBUG",
    "console_prints": "INFO",
    "message_sending": "WARNING"
  },
  "updater": {
    "repository": "tum-esm/ivy",
    "provider": "github",
    "provider_host": "github.com",
    "access_token": null,
    "source_conflict_behaviour": "reuse"
  },
  "backend": {
    "provider": "tenta",
    "mqtt_connection": {
      "host": "...",
      "port": 1883,
      "client_id": "...",
      "username": "...",
      "password": "..."
    },
    "max_parallel_messages": 50,
    "max_drain_time": 300
  },
  "dummy_procedure": {
    "seconds_between_datapoints": 10
  },
  "system_checks": {
    "seconds_between_checks": 120
  }
}

```

### Schema definition

<Schema schema={CONFIG_SCHEMA} />

## Foreign `config.json`

### Schema definition

<Schema schema={FOREIGN_CONFIG_SCHEMA} />
